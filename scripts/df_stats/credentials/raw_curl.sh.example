# 把从 Chrome DevTools 复制的 cURL 整段粘到这里，另存为 raw_curl.sh
# 步骤：
#   1. 打开 https://df.qq.com/cp/record202410ver/ ，扫码登录
#   2. F12 → Network 面板 → 切到战绩 tab 触发请求
#   3. 找到能返回 jData.data 数组的那条请求（路径里通常带 ide / fcg）
#   4. 右键 → Copy → Copy as cURL (bash)
#   5. 粘贴到 credentials/raw_curl.sh
#
# 示例（不能直接跑，仅说明格式）：
#
# curl 'https://comm.ams.game.qq.com/ide/?iChartId=319386&sIdeToken=zMemOt&type=4&page=1' \
#   -H 'Cookie: openid=oDj4xxx; acctype=wx; appid=101491592; access_token=93_xxx' \
#   -H 'Referer: https://df.qq.com/' \
#   --compressed
